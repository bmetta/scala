/**
 *  Following cases requires abstract class
 *  1) base class requires constructor arguments. Traits dont allow constructor parameters
 *  2) code will be called from Java code
 * 
 * Note: A class can extend only one abstract class
 *
 * Abstract class:
 *  works just like java
 *  some methods that have complete implementations, other methods that have no implementation
 */
object AbstractClassTest extends App {
}
